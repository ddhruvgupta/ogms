console.log("dataTables being used")



        $(document).ready(
          function() {
            //this function will execute after the page has finished loading
            $('#example').DataTable({});

        }
      );
